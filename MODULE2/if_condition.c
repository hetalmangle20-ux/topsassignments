#include<stdio.h>
/*
if (condition){
			//block
}
*/

//wap to find square of given num(only positive)

main(){
		int num;
		printf("\n enter the num:");
		scanf("%d", &num);
		if (num>0){
			printf ("/n square of %d=%d", num,num*num);
		}
   	}
